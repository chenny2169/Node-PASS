<!DOCTYPE html>
<% include header.ejs %>
<div class="container services">
<div class="jumbotron" >
<h1><%= title %></h1>        
</div>
<div class="container">
    <div class="row">
        <div class="span5">
            <table class="table table-striped table-condensed">
                <thead>
                    <tr>
                        <th>作業名稱</th>
                        <th>上傳情形</th>
                        <th>繳交期限</th>
                        <th>繳交時間</th>
                        <th>作業成績</th>       
                        <th>上傳</th>                                   
                    </tr>
                </thead>   
                <tbody>
                    <tr>
                        <%  for(var i=0; i < result.length; i++) { %>
                        <td><%= result[i].homeworkName %></td>
                        <td></td>
                        <td><%= result[i].dueDate %></td>
                        <td><%= result[i].submitTime %></td>
                        <td><%= result[i].studentID %></td>
                        <td><a class="btn btn-info" href="/listCourse/uploadPersonalHomework?homework_uuid=<%= result[i]._id %>&studentID=105598001" >上傳作業</a></td>                                       
                    </tr>
                    <% } %>             
                </tbody>
            </table>
        </div>
    </div>
</div>
</div>
<% include footer.ejs %>
